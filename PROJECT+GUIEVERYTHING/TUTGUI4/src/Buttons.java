public class Buttons {

    public static void main(String[] args) {
        
        new MainFrame(); // we could be creating a new object like MainFrame frame = new MainFrame();, but we don't need to do that since we don't need to give the name and create an object like that, we can just call the constructor.
    }
    
}
